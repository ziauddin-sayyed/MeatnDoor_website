query Categories {
	categories(level: 0, first: 100) {
		edges {
			node {
				id
				name
				slug
				children(first: 100) {
					edges {
						node {
							id
							name
							slug
						}
					}
				}
			}
		}
	}
}
